#include "main.h"
#include <stdio.h>
/**
 * Main - Entry point
 * @s: input
 * Return: Always 0 (Success)
 */

void _puts(char *s)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
